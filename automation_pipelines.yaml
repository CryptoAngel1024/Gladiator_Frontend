- service:     # Name of the service subscription
  teamKey:           # EAT Jenkins Team Key for the given Jenkins instance (ie. TABC)
  jenkinsType:        # EAT Jenkins Type. Supported values: dev, release
  environment:  # Automation Pipelines execution environment. Supported values: Production, Beta